import { writeFileSync } from 'fs';
import { name as _name, version as _version } from '../package.json'; // adjust the path as needed
/*
const packageInfo = {
  name: _name,
  version: _version
};

writeFileSync('../static/package.json', JSON.stringify(packageInfo, null, 2));*/